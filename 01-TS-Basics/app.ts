let courseName: string = "TS Fundamentals";
console.log(courseName);

/*
    1. starts with a lowercase, only class , interface , enum will starts with uppercase
    2. use camel case for more words in variables
    3. don't start with a number
    4. a-z , A-Z , 0-9 , _ , $
    5*. max chars length is 15
    6. for const use ALL_CAPS
 */

const APP_TITLE: string = "FaceBook Clone";
console.log(APP_TITLE);
